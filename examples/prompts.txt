# One prompt per line. Lines starting with # are ignored.
# These are sample INPUTS for the scope model to analyze and edit.
Write a Verilog testbench for module alu in alu.v that checks every opcode against a reference model and return the testbench file; keep the module interface unchanged.
hey can u make the riscv core work asap lol
Synthesize picorv32.v with Yosys for the iCE40 target and report LUT count, max clock frequency, and any warnings.
write me an essay about the history of computers
Refactor the fetch stage FSM in fetch.v to reduce state count, must not change the instruction timing, provide a patch and a summary of state encoding.
what's the weather
Lint all .v files in the rtl directory with verilator and list every warning grouped by file.
Explain how the CSR module in csr.v handles interrupt priority and document it in a short report.
debug the cache controller
Compare the pre- and post-synthesis netlists of aes_core.v for functional equivalence using Yosys and report any mismatches.
